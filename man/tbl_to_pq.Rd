% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_to_pq_dev.R
\name{tbl_to_pq}
\alias{tbl_to_pq}
\title{Export a lazy \code{dbplyr} table to Parquet via ADBC}
\usage{
tbl_to_pq(
  tbl,
  out_file,
  chunk_size = 100000L,
  metadata = NULL,
  col_types = NULL
)
}
\arguments{
\item{tbl}{A lazy table backed by \code{dbplyr}.}

\item{out_file}{Full path to the output Parquet file.}

\item{chunk_size}{Number of rows written per Parquet row group. Default is
\code{100000}.}

\item{metadata}{Optional named list of schema metadata to embed in the
Parquet file.}

\item{col_types}{Optional named list specifying Arrow type overrides. Values
may be string type names (for example \code{"int32"} or \code{"date"}) or Arrow
\code{DataType} objects.}
}
\value{
Invisibly returns \code{out_file}.
}
\description{
Renders a lazy \code{dbplyr} query to SQL, derives an ADBC PostgreSQL connection
from the backing \code{RPostgres} connection, and streams Arrow batches directly
to a Parquet file.
}
\details{
This is an experimental development path kept separate from
\code{\link{lazy_tbl_to_pq}} while we evaluate the ADBC transfer route. The ADBC path
opens a second PostgreSQL connection for the transfer, so the database must
allow an additional concurrent connection beyond the one already backing
\code{tbl}.
}
