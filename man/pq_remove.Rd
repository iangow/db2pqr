% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pq_archive_restore.R
\name{pq_remove}
\alias{pq_remove}
\title{Remove a Parquet file from active or archive storage}
\usage{
pq_remove(
  table_name = NULL,
  schema = NULL,
  data_dir = Sys.getenv("DATA_DIR", "."),
  file_name = NULL,
  archive = FALSE,
  archive_dir = "archive"
)
}
\arguments{
\item{table_name}{Name of the table. Required if \code{file_name} is not
provided.}

\item{schema}{Schema name. Required if \code{file_name} is not provided.}

\item{data_dir}{Root directory of the Parquet data repository. Defaults to
the \code{DATA_DIR} environment variable, or \code{"."} if unset.}

\item{file_name}{Full path to the Parquet file to remove. If provided,
\code{table_name}, \code{schema}, \code{data_dir}, and \code{archive} are
ignored.}

\item{archive}{If \code{TRUE}, resolve the file from the archive
subdirectory rather than the active schema directory.}

\item{archive_dir}{Name of the archive subdirectory relative to the schema
directory. Defaults to \code{"archive"}.}
}
\value{
Invisibly returns the path of the removed file, or \code{NULL} if
the file was not found or could not be deleted.
}
\description{
Deletes a Parquet file. By default the active file (in the schema directory)
is removed. Set \code{archive = TRUE} to remove a file from the archive
subdirectory instead.
}
\details{
Either \code{file_name} or both \code{table_name} and \code{schema} must be
provided.
}
\examples{
\dontrun{
pq_remove("company", "comp")
pq_remove("company_20251109T072042Z", "comp", archive = TRUE)
pq_remove(file_name = "~/pq_data/comp/company.parquet")
}

}
\seealso{
\code{\link{pq_archive}}, \code{\link{pq_restore}}
}
