% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{wrds_connect_dbi}
\alias{wrds_connect_dbi}
\title{Connect to WRDS PostgreSQL with DBI}
\usage{
wrds_connect_dbi(
  wrds_id = NULL,
  prompt = interactive(),
  save = TRUE,
  passfile = NULL
)
}
\arguments{
\item{wrds_id}{Optional WRDS username override.}

\item{prompt}{If \code{TRUE}, prompt interactively for a WRDS PostgreSQL password
when no password was supplied and no matching \code{.pgpass} entry exists.}

\item{save}{If \code{TRUE}, save a supplied or prompted password to \code{.pgpass}
after a successful WRDS connection test.}

\item{passfile}{PostgreSQL password-file path. Defaults to \code{PGPASSFILE} when
set, otherwise the platform default password-file path.}
}
\value{
A \code{DBIConnection} object.
}
\description{
Opens a \code{DBI} connection to the WRDS PostgreSQL database using \code{RPostgres}
and the \code{db2pq} WRDS authentication flow.
}
\details{
The username is resolved from \code{wrds_id}, \code{WRDS_ID}, \code{WRDS_USER}, or the
keyring entry written by \code{wrds::wrds_set_credentials()}. Password handling
prefers a matching PostgreSQL \code{.pgpass} entry, then \code{WRDS_PASSWORD}, then an
interactive password prompt when \code{prompt = TRUE}. When a supplied, prompted,
or \code{WRDS_PASSWORD} password is used successfully and \code{save = TRUE}, it is
saved to \code{.pgpass} for future connections.
}
\examples{
\dontrun{
# Requires WRDS credentials and network access to WRDS PostgreSQL
con <- wrds_connect_dbi()
DBI::dbDisconnect(con)
}
}
