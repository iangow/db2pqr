% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_to_pq.R
\name{db_to_pq}
\alias{db_to_pq}
\title{Export a PostgreSQL table to Parquet}
\usage{
db_to_pq(
  table_name,
  schema,
  host = Sys.getenv("PGHOST", "localhost"),
  database = Sys.getenv("PGDATABASE", unset = Sys.getenv("PGUSER")),
  user = Sys.getenv("PGUSER", Sys.info()[["user"]]),
  password = Sys.getenv("PGPASSWORD", ""),
  port = as.integer(Sys.getenv("PGPORT", 5432)),
  data_dir = Sys.getenv("DATA_DIR", "."),
  out_file = NULL,
  where = NULL,
  obs = NULL,
  keep = NULL,
  drop = NULL,
  rename = NULL,
  alt_table_name = NULL,
  chunk_size = 100000L,
  transfer_method = c("dbi", "adbc"),
  numeric_mode = c("decimal", "float64", "text", "raw"),
  con = NULL,
  metadata = NULL,
  col_types = NULL,
  tz = NULL
)
}
\arguments{
\item{table_name}{Name of the source PostgreSQL table.}

\item{schema}{Name of the source PostgreSQL schema.}

\item{host, database, user, password, port}{PostgreSQL connection parameters
used when \code{con} is not supplied.}

\item{data_dir}{Root directory of the local Parquet data repository. Defaults
to \code{DATA_DIR}, or \code{"."} if unset.}

\item{out_file}{Optional full output path. Overrides \code{data_dir}, \code{schema},
and \code{table_name}.}

\item{where}{Optional SQL \code{WHERE} clause without the \code{WHERE} keyword.}

\item{obs}{Optional integer row limit.}

\item{keep, drop}{Optional character vectors of regex patterns used to select
source columns. \code{drop} is applied before \code{keep}.}

\item{rename}{Optional named character vector or list mapping source column
names to output column names, e.g. \code{c(conm = "company_name")}.}

\item{alt_table_name}{Optional output basename when \code{out_file} is omitted.}

\item{chunk_size}{Number of rows fetched and written per chunk.}

\item{transfer_method}{Transfer backend: \code{"dbi"} for the stable DBI path or
\code{"adbc"} for the optional Arrow/ADBC path.}

\item{numeric_mode}{Numeric handling mode for PostgreSQL \code{numeric} columns.
The default \code{"decimal"} transfers them as text and writes bounded
\code{numeric(p, s)} columns as Arrow decimal Parquet columns. \code{"float64"} casts
them to \verb{DOUBLE PRECISION} before transfer. \code{"text"} transfers them as text
without recreating decimal types. \code{"raw"} keeps the backend default.}

\item{con}{Optional existing DBI connection.}

\item{metadata}{Optional named list of Parquet schema metadata.}

\item{col_types}{Optional named list of Arrow output type overrides. Names
refer to output column names after \code{rename}.}

\item{tz}{Time zone used to interpret \verb{TIMESTAMP WITHOUT TIME ZONE} columns.}
}
\value{
Invisibly returns the output file path.
}
\description{
Exports a table from PostgreSQL to a Parquet file, with optional row
filtering, column filtering, column renaming, Arrow type overrides, and
timestamp normalization.
}
