% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_to_pq_dev.R
\name{con_to_adbi}
\alias{con_to_adbi}
\title{Create an ADBC PostgreSQL connection from an \code{RPostgres} connection}
\usage{
con_to_adbi(con)
}
\arguments{
\item{con}{An \code{RPostgres} connection object.}
}
\value{
An ADBC connection created by \code{adbi}.
}
\description{
This helper is intentionally narrow: it only supports \code{RPostgres}
\code{PqConnection} objects and assumes enough connection information is available
to build a PostgreSQL URI. If any required piece is missing, the function
fails instead of attempting alternative fallbacks. The returned ADBC
connection is separate from the original \code{RPostgres} connection, so using
this helper requires capacity for an additional concurrent database
connection.
}
\examples{
\dontrun{
# Requires a PostgreSQL connection and the adbi and adbcpostgresql packages
pg_con <- DBI::dbConnect(RPostgres::Postgres())
adbi_con <- con_to_adbi(pg_con)
DBI::dbDisconnect(adbi_con)
DBI::dbDisconnect(pg_con)
}
}
