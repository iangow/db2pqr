% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark_tbl_to_pq.R
\name{benchmark_tbl_to_pq}
\alias{benchmark_tbl_to_pq}
\title{Benchmark Parquet export paths for a lazy \code{dbplyr} table}
\usage{
benchmark_tbl_to_pq(
  tbl,
  methods = c("lazy_tbl_to_pq", "tbl_to_pq", "parquetize"),
  out_dir = tempdir(),
  chunk_size = 100000L,
  parquetize_max_rows = chunk_size,
  metadata = NULL,
  col_types = NULL
)
}
\arguments{
\item{tbl}{A lazy table backed by \code{dbplyr}.}

\item{methods}{Character vector of methods to benchmark. Defaults to all
supported methods.}

\item{out_dir}{Directory used for temporary output files.}

\item{chunk_size}{Chunk size used by \code{\link[=lazy_tbl_to_pq]{lazy_tbl_to_pq()}} and \code{\link[=tbl_to_pq]{tbl_to_pq()}}.}

\item{parquetize_max_rows}{Chunk size used by \code{\link[parquetize:dbi_to_parquet]{parquetize::dbi_to_parquet()}}.
Defaults to \code{chunk_size}.}

\item{metadata}{Optional named list of schema metadata passed to
\code{\link[=lazy_tbl_to_pq]{lazy_tbl_to_pq()}} and \code{\link[=tbl_to_pq]{tbl_to_pq()}}.}

\item{col_types}{Optional named list of Arrow type overrides passed to
\code{\link[=lazy_tbl_to_pq]{lazy_tbl_to_pq()}} and \code{\link[=tbl_to_pq]{tbl_to_pq()}}.}
}
\value{
A data frame with one row per benchmarked method.
}
\description{
Runs one or more table-to-Parquet export methods serially on the same lazy
\code{dbplyr} query and returns a comparison table with elapsed time, memory use,
output row count, and file size.
}
\details{
Supported methods are:
\itemize{
\item \code{"lazy_tbl_to_pq"} for the package's existing DBI/data-frame path
\item \code{"tbl_to_pq"} for the experimental ADBC/Arrow path
\item \code{"parquetize"} for \code{\link[parquetize:dbi_to_parquet]{parquetize::dbi_to_parquet()}}
}

The methods are always run in series to avoid overlapping database, network,
or disk load.
}
