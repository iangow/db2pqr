% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/auth.R
\name{wrds_credentials_available}
\alias{wrds_credentials_available}
\title{Check whether WRDS credentials are available}
\usage{
wrds_credentials_available(
  wrds_id = NULL,
  password = NULL,
  prompt = interactive(),
  passfile = NULL
)
}
\arguments{
\item{wrds_id}{Optional WRDS username override.}

\item{password}{Optional password. If omitted, a matching \code{.pgpass} entry is
preferred. When \code{.pgpass} has no matching entry, an existing
\code{WRDS_PASSWORD} environment variable is used before an interactive prompt.
PostgreSQL may also use \code{PGPASSWORD} or another libpq-supported mechanism
when no password is passed to the connection.}

\item{prompt}{If \code{TRUE}, prompt interactively for a WRDS PostgreSQL password
when no password was supplied and no matching \code{.pgpass} entry exists.}

\item{passfile}{PostgreSQL password-file path. Defaults to \code{PGPASSFILE} when
set, otherwise the platform default password-file path.}
}
\value{
\code{TRUE} if \code{wrds_check_credentials()} can open a WRDS PostgreSQL
connection, otherwise \code{FALSE}.
}
\description{
Check whether WRDS credentials are available
}
\examples{
wrds_credentials_available(prompt = FALSE)
}
